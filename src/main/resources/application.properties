spring.application.name=products-service

# server
server.servlet.context-path=${HTTP_BASE_PATH:/v1/products}
server.max-http-request-header-size=${HTTP_MAX_HEADER_SIZE:8KB}

# management
management.server.base-path=${HTTP_MANAGEMENT_BASE_PATH:/v1/products}
management.server.port=9090
management.endpoints.web.exposure.include=health,prometheus,openapi,swagger-ui

# config
spring.config.import=optional:configserver:${CONFIG_SERVER_URL}

# springdoc
springdoc.use-management-port=true

# datasource
spring.datasource.url=${DATASOURCE_URL}
spring.datasource.username=${DATASOURCE_USERNAME}
spring.datasource.password=${DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# mongodb
spring.data.mongodb.host=${MONGODB_HOST}
spring.data.mongodb.port=${MONGODB_PORT:27017}
spring.data.mongodb.username=${MONGODB_USERNAME}
spring.data.mongodb.password=${MONGODB_PASSWORD}
spring.data.mongodb.authentication-database=${MONGODB_AUTHENTICATION_DATABASE:admin}
spring.data.mongodb.database=${MONGODB_DATABASE:products}
spring.data.mongodb.uuid-representation=standard

# kafka
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS}
spring.kafka.consumer.group-id=${KAFKA_CONSUMER_GROUP_ID:products-service}
